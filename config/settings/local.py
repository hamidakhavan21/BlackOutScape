# flake8: noqa

from .base import *

DEBUG = True

ALLOWED_HOSTS = ["*"]

# easier dev logging
LOGGING = {
    "version": 1,
    "disable_existing_loggers": False,
    "handlers": {
        "console": {
            "class": "logging.StreamHandler",
        },
    },
    "root": {
        "handlers": ["console"],
        "level": "INFO",
    },
}
